'use strict';

describe('myApp.persona', function() {

  beforeEach(module('ui.sortable'));

  describe('view2 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('PersonaCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});